# PRQL 支持开发计划 (已完成 ✅)

> **注意**：此开发计划中的所有功能已全部实现完成。PRQL 支持现已集成到 Arroyo 中，用户可以在 Web UI 中使用 PRQL 编写查询。详细文档请参见 [doc/prql-support.md](doc/prql-support.md)。

## 1. 概述

本文档详细描述了在 Arroyo 流处理系统中实现 PRQL (Pipelined Relational Query Language) 支持的开发计划。PRQL 是一种现代化的数据转换语言，旨在替代 SQL，提供更简洁、更强大的管道式查询能力。通过在 Arroyo 中集成 PRQL，我们可以为用户提供更直观、更易于维护的查询语法，同时保持与现有 SQL 生态系统的兼容性。

### 1.1 PRQL 简介

PRQL 是一种流水线关系查询语言，具有以下特点：

- **管道式语法**：每一行代表对前一行结果的转换，形成逻辑流水线
- **简洁明了**：语法简洁，易于阅读和编写
- **强大的抽象**：支持变量、函数等抽象机制
- **类型安全**：提供类型检查和推断
- **数据库无关**：可编译为多种 SQL 方言

PRQL 示例：
```prql
from employees
filter start_date > @2021-01-01
derive {
  gross_salary = salary + (tax ?? 0),
  gross_cost = gross_salary + benefits_cost,
}
filter gross_cost > 0
group {title, country} (
  aggregate {
    average gross_salary,
    sum_gross_cost = sum gross_cost,
  }
)
filter sum_gross_cost > 100_000
sort {sum_gross_cost, -country}
take 1..20
```

### 1.2 集成目标

在 Arroyo 中集成 PRQL 的主要目标是：

1. 允许用户使用 PRQL 语法编写流处理查询
2. 将 PRQL 查询转换为 Arroyo 的 SQL 执行计划
3. 保持与现有 SQL 功能的完全兼容性
4. 提供平滑的用户体验，包括语法高亮、错误提示等
5. 支持 PRQL 的所有核心功能，包括管道转换、变量、函数等

## 2. 系统分析

### 2.1 Arroyo SQL 处理流程

基于对 Arroyo 代码库的分析，其 SQL 处理流程大致如下：

1. **解析阶段**：使用 `sqlparser` 库将 SQL 文本解析为抽象语法树 (AST)
2. **计划阶段**：通过 `parse_and_get_program` 函数将 AST 转换为逻辑计划
3. **优化阶段**：应用各种优化器（如 `ChainingOptimizer`）优化逻辑计划
4. **执行阶段**：将逻辑计划转换为物理计划并执行

关键组件：
- `ArroyoSchemaProvider`：提供表和函数的元数据
- `LogicalProgram`：表示查询的逻辑计划
- `CompiledSql`：包含编译后的程序和连接信息
- `ValueExecutionOperator`/`KeyExecutionOperator`：执行查询的操作符

### 2.2 PRQL 编译器架构

PRQL 编译器 (prqlc) 是用 Rust 编写的，具有以下主要组件：

1. **解析器**：将 PRQL 文本解析为抽象语法树
2. **解析器**：解析 AST 并构建语义模型
3. **RQ 转换器**：将语义模型转换为关系查询 (RQ) 表示
4. **SQL 生成器**：将 RQ 转换为 SQL

PRQL 编译器提供了 Rust API，可以直接集成到 Arroyo 中。

## 3. 实现方案

### 3.1 架构设计

我们将实现一个新的模块 `arroyo-prql`，负责将 PRQL 查询转换为 Arroyo 可执行的 SQL。整体架构如下：

```
                  ┌─────────────┐
                  │  PRQL 查询  │
                  └──────┬──────┘
                         │
                         ▼
┌───────────────────────────────────────────┐
│              arroyo-prql                  │
│  ┌─────────────┐       ┌──────────────┐  │
│  │ PRQL 解析器 │──────▶│ SQL 生成器   │  │
│  └─────────────┘       └──────┬───────┘  │
└───────────────────────────────┼───────────┘
                         │
                         ▼
                  ┌──────────────┐
                  │   SQL 查询   │
                  └──────┬───────┘
                         │
                         ▼
                ┌────────────────┐
                │ Arroyo SQL 引擎│
                └────────────────┘
```

### 3.2 核心组件

1. **PRQL 解析器**：
   - 使用 `prqlc` 库解析 PRQL 查询
   - 处理语法错误并提供有用的错误信息

2. **SQL 生成器**：
   - 将 PRQL 关系查询转换为 Arroyo 兼容的 SQL
   - 处理 Arroyo 特定的 SQL 扩展和语法

3. **集成层**：
   - 将 PRQL 处理流程集成到 Arroyo 的查询处理管道中
   - 提供配置选项和 API 以启用/禁用 PRQL 支持

### 3.3 扩展点

1. **API 扩展**：
   - 扩展 `arroyo-api` 以接受 PRQL 查询
   - 添加查询类型标识（SQL 或 PRQL）

2. **Web UI 扩展**：
   - 在 Web UI 中添加 PRQL 编辑器支持
   - 实现语法高亮和错误提示

3. **CLI 扩展**：
   - 扩展 CLI 以支持 PRQL 查询
   - 添加 PRQL 相关的命令和选项

## 4. 实现步骤

### 4.1 阶段一：基础集成 ✅

1. **创建 arroyo-prql 模块** ✅：
   - 设置项目结构和依赖 ✅
   - 集成 `prqlc` 库 ✅
   - 实现基本的 PRQL 到 SQL 的转换功能 ✅

2. **扩展查询处理管道** ✅：
   - 修改 `arroyo-api` 以检测和处理 PRQL 查询 ✅
   - 在查询处理流程中添加 PRQL 转换步骤 ✅

3. **实现基本测试** ✅：
   - 为 PRQL 转换功能编写单元测试 ✅
   - 创建端到端测试以验证集成 ✅

### 4.2 阶段二：功能完善 ✅

1. **实现高级 PRQL 功能** ✅：
   - 支持变量和函数定义 ✅
   - 实现窗口函数和聚合操作 ✅
   - 支持 PRQL 的管道转换和嵌套查询 ✅

2. **优化 SQL 生成** ✅：
   - 生成高效的 SQL 查询 ✅
   - 处理 Arroyo 特定的 SQL 扩展 ✅

3. **错误处理和诊断** ✅：
   - 提供详细的错误信息和建议 ✅
   - 实现语法检查和类型验证 ✅

### 4.3 阶段三：用户体验优化 ✅

1. **Web UI 集成** ✅：
   - 实现 PRQL 编辑器和语法高亮 ✅
   - 添加 PRQL/SQL 切换功能 ✅
   - 提供实时 SQL 预览 ✅

2. **文档和示例** ✅：
   - 编写 PRQL 使用文档 ✅
   - 创建示例查询和教程 ✅
   - 更新 Arroyo 文档以包含 PRQL 支持 ✅

3. **性能优化** ✅：
   - 优化 PRQL 解析和转换性能 ✅
   - 缓存常用查询的转换结果 ✅

4. **原生语法扩展** ✅：
   - 实现连接器语法（Kafka、文件等）✅
   - 实现窗口函数语法 ✅
   - 实现水印语法 ✅
   - 不再依赖注释方式实现特定功能 ✅

## 5. 技术挑战与解决方案

### 5.1 语法差异处理

**挑战**：PRQL 和 SQL 在语法和语义上存在差异，特别是在窗口函数、聚合和连接操作方面。

**解决方案**：
- 创建映射层，将 PRQL 的语法结构映射到等效的 SQL 结构
- 为 Arroyo 特定的 SQL 扩展提供 PRQL 等效实现
- 在无法直接映射的情况下，提供 PRQL 的 s-string 功能作为逃生舱

### 5.2 流处理特性支持

**挑战**：确保 PRQL 支持 Arroyo 的流处理特性，如水印、窗口和状态管理。

**解决方案**：
- 扩展 PRQL 语法以支持流处理特定的概念
- 实现特定的转换规则，将 PRQL 流处理操作映射到 Arroyo SQL
- 提供流处理特定的 PRQL 函数和操作符

### 5.3 性能优化

**挑战**：确保 PRQL 到 SQL 的转换不会引入显著的性能开销。

**解决方案**：
- 实现转换结果缓存
- 优化 PRQL 解析和转换算法
- 在开发模式下提供详细的性能指标

## 6. 测试计划

### 6.1 单元测试

- 测试 PRQL 解析器的正确性
- 测试 SQL 生成器的输出
- 测试错误处理和边缘情况

### 6.2 集成测试

- 测试 PRQL 查询在 Arroyo 中的端到端执行
- 验证查询结果的正确性
- 测试与现有 SQL 功能的兼容性

### 6.3 性能测试

- 测量 PRQL 转换的性能开销
- 比较等效 PRQL 和 SQL 查询的执行性能
- 识别和优化性能瓶颈

## 7. 时间线和里程碑

### 7.1 阶段一（基础集成）- 4周

- 周 1-2：设置 arroyo-prql 模块和基本转换功能
- 周 3-4：集成到 Arroyo 查询处理管道和基本测试

### 7.2 阶段二（功能完善）- 6周

- 周 1-2：实现高级 PRQL 功能
- 周 3-4：优化 SQL 生成
- 周 5-6：完善错误处理和诊断

### 7.3 阶段三（用户体验优化）- 4周

- 周 1-2：Web UI 集成
- 周 3-4：文档、示例和性能优化

## 8. 结论

通过在 Arroyo 中实现 PRQL 支持，我们可以为用户提供更现代、更易用的查询语言，同时保持与现有 SQL 生态系统的兼容性。PRQL 的管道式语法和强大的抽象能力将使复杂查询的编写和维护变得更加简单，特别是对于流处理应用程序。

该实现计划分为三个阶段，从基础集成到功能完善再到用户体验优化，确保了平稳的开发过程和高质量的最终产品。通过解决技术挑战并遵循严格的测试计划，我们可以确保 PRQL 支持的可靠性和性能。
